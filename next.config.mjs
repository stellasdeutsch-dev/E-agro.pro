/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/E-agro.pro',
  assetPrefix: '/E-agro.pro',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
